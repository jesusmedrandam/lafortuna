import { Router } from 'express';
import { z } from 'zod';
import { pool } from '../../database/pool.js';
import { transaction } from '../../database/transaction.js';
import { asyncHandler } from '../../core/async-handler.js';
import { routeParam } from '../../core/route-param.js';
import { created, ok } from '../../core/http.js';
import { NotFoundError } from '../../core/errors.js';
import { requirePermission } from '../../middleware/permission.js';
import { buildInsert } from '../shared/sql.js';
const schema=z.object({id_tipo_tratamiento:z.string().uuid(),id_medicamento:z.string().uuid(),id_via_administracion:z.string().uuid(),dosis_general:z.number().positive(),id_unidad_dosis:z.string().uuid(),modo_seleccion:z.enum(['TODOS','GRUPO','SELECCION_MANUAL']),id_grupo_filtro:z.string().uuid().nullable().optional(),fecha_aplicacion:z.string().datetime(),responsable:z.string().max(200).nullable().optional(),observaciones:z.string().nullable().optional(),animales:z.array(z.object({id_animal:z.string().uuid(),seleccionado:z.boolean().default(true),dosis_aplicada:z.number().positive().nullable().optional(),id_unidad_dosis:z.string().uuid().nullable().optional(),observaciones:z.string().max(300).nullable().optional()})).min(1)});
export const sanitaryRouter=Router();
sanitaryRouter.get('/',requirePermission('SANIDAD_CONSULTAR'),asyncHandler(async(_req,res)=>ok(res,(await pool.query(`SELECT j.*,tt.nombre tipo_tratamiento,m.nombre_comercial medicamento,v.nombre via,u.simbolo unidad,COALESCE((SELECT jsonb_agg(jsonb_build_object('id_detalle',d.id_jornada_detalle,'id_animal',a.id_animal,'animal',a.nombre,'seleccionado',d.seleccionado,'dosis_aplicada',d.dosis_aplicada,'estado',d.estado)) FROM jornada_sanitaria_detalle d JOIN animal a ON a.id_animal=d.id_animal WHERE d.id_jornada=j.id_jornada AND d.deleted_at IS NULL),'[]') detalles FROM jornada_sanitaria j JOIN tipo_tratamiento tt ON tt.id_tipo_tratamiento=j.id_tipo_tratamiento JOIN medicamento m ON m.id_medicamento=j.id_medicamento JOIN via_administracion v ON v.id_via_administracion=j.id_via_administracion JOIN unidad_medida u ON u.id_unidad=j.id_unidad_dosis WHERE j.deleted_at IS NULL ORDER BY j.fecha_aplicacion DESC`)).rows)));
sanitaryRouter.post('/',requirePermission('SANIDAD_ADMINISTRAR'),asyncHandler(async(req,res)=>{const x=schema.parse(req.body);const result=await transaction(async c=>{const {animales,...head}=x;const j=(await c.query(buildInsert('jornada_sanitaria',{...head,estado:'BORRADOR',total_candidatos:animales.length,total_seleccionados:animales.filter(a=>a.seleccionado).length,registrado_por:req.user!.id}))).rows[0];for(const a of animales)await c.query(buildInsert('jornada_sanitaria_detalle',{...a,id_jornada:j.id_jornada}));return j;},req.user!.id);return created(res,result);}));
sanitaryRouter.put('/:id/seleccion',requirePermission('SANIDAD_ADMINISTRAR'),asyncHandler(async(req,res)=>{const items=schema.shape.animales.parse(req.body.animales);await transaction(async c=>{const found=await c.query('SELECT estado FROM jornada_sanitaria WHERE id_jornada=$1 AND deleted_at IS NULL FOR UPDATE',[routeParam(req.params.id, 'id')]);if(!found.rows[0])throw new NotFoundError();if(found.rows[0].estado!=='BORRADOR')throw new Error('Solo puede editarse una jornada en borrador.');await c.query('DELETE FROM jornada_sanitaria_detalle WHERE id_jornada=$1',[routeParam(req.params.id, 'id')]);for(const a of items)await c.query(buildInsert('jornada_sanitaria_detalle',{...a,id_jornada:routeParam(req.params.id, 'id')}));await c.query('UPDATE jornada_sanitaria SET total_candidatos=$2,total_seleccionados=$3 WHERE id_jornada=$1',[routeParam(req.params.id, 'id'),items.length,items.filter(x=>x.seleccionado).length]);},req.user!.id);return ok(res,{message:'Selección actualizada.'});}));
sanitaryRouter.post('/:id/aplicar',requirePermission('SANIDAD_ADMINISTRAR'),asyncHandler(async(req,res)=>ok(res,(await pool.query('SELECT aplicar_jornada_sanitaria($1,$2) cantidad',[routeParam(req.params.id, 'id'),req.user!.id])).rows[0])));
